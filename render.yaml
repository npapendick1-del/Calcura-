services:
  - type: web
    name: meisterki
    env: node
    buildCommand: "npm install && npm run build"
    startCommand: "npm start"
    plan: free
    envVars:
      - key: NODE_VERSION
        value: 22.16.0
