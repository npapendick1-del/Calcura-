{
  "name": "meisterki",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "install": "npm --prefix server install",
    "build": "npm --prefix server run build",
    "start": "node server/dist/index.js"
  }
}
